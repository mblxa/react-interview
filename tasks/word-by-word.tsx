import React from "react";

/**
 * Write a functional component that accepts an extended piece of text
 * from the user and prints the text to the screen, beginning with the first word
 * and appending the next word every half-second until the entire text is
 * displayed on the screen.
 *
 * For example, if the user submits “Hi my name is Bob”,
 * the screen should read “Hi”, then “Hi my”, then “Hi my name”, and so on.
 * If the user submits another piece of text reset the display
 * and begin printing the new text.
 */

function App() {
    return <WordByWord />;
}

const WordByWord = () => {
    // YOUR CODE HERE
};

export default App;
